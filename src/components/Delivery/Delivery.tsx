import React from 'react';

import { DeliveryButton } from './Delivery.styles';

const Delivery = () => {
    return (
        <DeliveryButton>
            
        </DeliveryButton>
    );
};

export default Delivery;
